kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: cp4a-shared-log-pvc
  namespace: cp4ba
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: ocs-storagecluster-cephfs
